'use strict';

module.exports = function (handlers) {
  return [
/*  {
      method: 'POST',
      path: '/login',
      config: {
        handler: handlers.login.POST,
        auth: { mode: 'try' }
      }
    } */
  ];
};