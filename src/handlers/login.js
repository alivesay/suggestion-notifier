'use strict';

module.exports = {
/*
  POST: function (request, reply) {
    request.server.methods.auth.login(request.payload, function onLoginDone (...) {
      if (!err) {
        return reply();
      }

      return reply(err);
    });
  }
*/
};